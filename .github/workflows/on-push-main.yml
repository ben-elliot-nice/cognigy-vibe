name: Release (production)

on:
  push:
    branches: [main]

jobs:
  release:
    permissions:
      contents: write
    uses: ./.github/workflows/_release.yml
    with:
      prerelease: false
      environment: production
    secrets: inherit
